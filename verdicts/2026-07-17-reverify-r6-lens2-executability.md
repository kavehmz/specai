# Blind re-verification round 6 — lens 2: executability/buildability (verbatim report)

*Preserved verbatim per `verification_core.md` §6 rule 4. Dispatcher: the operator by standing
order; lead as mechanical proxy. Package: the full tree minus `verdicts/` and `.git`.*

---

**DISCLOSURE — ambient context beyond this prompt and the scoped files:** a system-reminder (user email, date); an environment block including a git-status snapshot with recent commit subjects; tool/skill listings; and a `find` listing that exposed `verdicts/` filenames (contents unread). Nothing else.

# VERDICT: FAIL — 0 blockers, 4 majors, 4 minors

## Majors

**1. verification_core.md §4 M row: the templated M-tier seal split omits the buildability lens entirely** — a zero-discretion dispatcher following the default never dispatches one of "the two lenses that decide most seals"; nothing recovers it at M. Fix: add buildability to one half.

**2. verification_core.md §4 L row: no templated lens allocation and no rule that the panel's union must cover all applicable lenses** — the dispatcher is forced to invent the split, exactly the discretion rule 4 forbids. Fix: a named default allocation or the union rule.

**3. phases/delta.md step 1 vs fenced check 2: the definition delta mandates no spot-verify, but the check derives an expected record for every re-run phase** — at a tier-L delta, a guaranteed false finding or an undeclared duty. Fix: add step 2's clause ("spot-verify where the schedule fires") to step 1.

**4. phases/delta.md step 2 + architecture step 10: the delta architecture spot-verify cannot be dispatched as written** — its declared package names the original run's full inventory (absent from the delta's fresh run dir) and its lens list says ideation coverage with no change-request substitution; step 6's substitution table scopes itself to the seal only. Fix: extend step 2 with the spot-verify substitutions.

## Minors

5 the delta package's consumer-ledger.md has no producer when no contracts phase ran — name it conditional. 6 the package includes spot-verify records while ending "Never: prior reports" — say "prior seal-round reports". 7 the eval preamble's IOU sweep has no named recording home. 8 the comparison "panel" has no size floor (N=1 satisfies it).

## Clean walks

S-run producer/consumer chain complete. Reg/money chain closed and dimension-keyed. Phase-6 seal dispatch at S completes as written (incl. delta/eval readings, count=0 at S). Mainline spot-verifies dispatch-legal. Crash-resume holds at every constructible interruption point. Eval route end-to-end complete and self-consistent (staging, preamble, prompt, exclusions, eval-internal seal exception, operator-dispatched comparison + build proof, operator/registrar close-out). Emitted-repo operability from templates alone — the operator-commits rule reaches the builder verbatim. The engineering-standard inline-at-S path coherent across five homes.

The four majors share one shape the ledger itself predicted: hardenings that rode the main path and stopped one file short of the sibling.
