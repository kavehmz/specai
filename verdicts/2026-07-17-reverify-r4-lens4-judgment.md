# Blind re-verification round 4 — lens 4: judgment gate + adversarial (verbatim report)

*Preserved verbatim per `verification_core.md` §6 rule 4. Dispatcher: the operator by standing
order; lead as mechanical proxy. Package: the full tree minus `verdicts/` and `.git`. Brief note:
this round's judgment brief carried the operator's recorded acceptance (gate baselines lead-held;
per-gate commits declined) with an accepted-class severity for findings inside it — disclosed
here per §2 rule 4's deviation rule.*

---

**DISCLOSURE — ambient context beyond the prompt and listed files:** (1) a system-reminder with a user email and today's date; (2) an environment header including a git-status snapshot of this repo (branch, clean status, recent commit subjects); (3) tool/skill/agent listings. I read nothing outside the 21 listed files. The commit subjects gave me no finding.

**LENS VERDICT: FAIL** — 1 blocker, 3 majors (plus minors and accepted-class residue).

**F1 — BLOCKER · phases/verification.md (verifier package) vs exemplars.md §3 rule 2:** the phase-6 package hands specai's NOTES.md — the eval answer key — to every seal's verifiers, with no eval carve-out; an eval run's internal phase-6 seal is a factory seal, so the letter mandates giving the run's seal verifiers the ledger carrying the fixtures' facts; findings feed the run's fix loop, so answer-key knowledge flows into the candidate. The exclusion overrides only SKILL §0, not this package line; the IOU sweep at an eval's internal seal is also unsatisfiable. Fix: condition the package item ("except during an eval"); the IOU sweep moves to the operator's eval preamble; mirror the override in exemplars §3 rule 2.

**F2 — MAJOR · eval-run dispatch:** the eval run itself has no templated brief, no preserved dispatch prompt, and no defined way to learn it is under eval; the exclusion list lives chiefly in the excluded file; binding docs invite the read (emission's optional-calibration pointer). Concrete cheat outside the recorded acceptance: the lead dispatches the eval run with a prompt omitting the eval framing; no rule preserves that prompt (attestation covers verifiers only; the run is an author). Fix: a fenced, verbatim eval dispatch prompt in exemplars §3, preserved in the run dir; operator stages or verifies a scrubbed run environment.

**F3 — MAJOR · verification_core §2 rule 4:** the attestation echo is unteachable to the verifiers it binds — rule 4 is excluded from its own closed brief, and spot-verify/comparison packages don't contain verification_core.md. Fix: add the attestation sentence to the conforming-brief enumeration or the file to every package.

**F4 — MAJOR · phases/delta.md step 6:** the delta path escapes the round-3 mitigations — no gates/ in the package (no baseline diff), no spot-verify record count, no IOU/exempted-edit sweep; delta's own gates never state the gate-copy duty; change_rule §5.4's collector list omits delta seals. Not mere lead-trust residue: the mitigations the acceptance relies on are absent on this path. Fix: extend delta's gates and step-6 package/duties; add delta seals to the collector list.

**Minors:** M1 residual fuzzy fixture echoes (dial "eight-component tree"; definition "page … multi-thousand-line suite") — NOTES' triage claim notwithstanding. M2 the run-close seed append has no defined transform ("distilled" = lead discretion; "truncated" cut-point discretionary). M3 the registrar disclosure lives only in the deletable journal — mirror it in the NOTES stamp. M4 mechanical-pass results never reach the verdict record. M5 the substantive-vs-clarity classification on self-governing surfaces is unaudited. M6 degraded-mode evals never explicitly prohibited.

**Accepted-class (inside the recorded acceptance; reported, not failing):** A1 gate copies, records, IOU ledger, and compilations are lead-written — wholesale fabrication possible, fully visible as content in the countersign diff — squarely the accepted residual. A2 verifier packages are lead-staged paths; the preserved dispatch prompt would show anomalies at commit review.

**Judgment note:** the tree is coherent, genuinely teaches, and the ledger's honesty is real (it records its own false "quarantine completed" claim). The ledger and binding text now agree on the rules in force (IOU-payment letter fixed; both triage adoptions present). The failure mode left is consistent and instructive: each hardening round patched the main path and left a sibling path (eval seals, delta seals, spot-verify briefs) running the old rules — the fix class for F1–F4 is "enumerate every path the rule must ride," not new rules.
