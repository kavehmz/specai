# Blind re-verification round 6 — lens 4: judgment gate + adversarial (verbatim report)

*Preserved verbatim per `verification_core.md` §6 rule 4. Dispatcher: the operator by standing
order; lead as mechanical proxy. Package: the full tree minus `verdicts/` and `.git`. Brief
carried the operator's recorded acceptances with accepted-class severity, disclosed per §2
rule 4.*

---

**DISCLOSED ambient context beyond the prompt and listed files:** a system-reminder carrying the user's email and today's date, and a git-status snapshot naming recent commit subjects. I did not open verdicts/, .git, or any unlisted file.

# VERDICT: FAIL — 3 majors, 0 blockers

**Ledger↔tree agreement:** checked the round-6 entry's claims specifically — delta-seal-by-named-substitution ✓, gates/** diff ✓, eval close-out as operator/registrar act ✓, taxonomy carve-out ✓, commit rule in the emitted start template ✓, seal-operating text panel-gated ✓. The quarantine-letter refinement holds under a grep of all binding docs. The open IOU is stated open under the corrected payment letter. The ledger is honest. The tree is coherent and teachable, at the acknowledged edge of prose density.

## Findings

**1. MAJOR — verification_core.md §4: no rule requires seal lens totality; the M default split omits buildability.** A lead dispatching mechanically follows the M default literally and never runs the rebuild test; at L the letter permits sealing on 3 lenses. Fix: add buildability to the M split, and one sentence: at every tier the union of dispatched lens briefs MUST equal the applicable §5 set; a lens with no dispatched carrier blocks the seal.

**2. MAJOR — phases/delta.md step 6: the delta seal's lens set drops outcomes, adversarial-user, lifecycle, and altitude — a sibling-path gap of exactly the round-4 class.** A delta adding a money formula seals with no independent recomputation; change_rule §4's top-tier gating raises verifier count, not lens set. Fix: all applicable §5 lenses, scoped to the changed set plus the derived cascade seam, with change-request coverage replacing ideation coverage.

**3. MAJOR — the tier-L per-run taxonomy has no enforcement surface, and the cheat is invisible even in the commit-time diff (escapes the recorded acceptance).** Verifiers attest the brief and file list, never the taxonomy's contents; the source records are not in the product-seal package; taxonomy.md never relocates to a committed home, so the diff backstop never sees it. A lead can silently omit the seed lines describing its own recurring defect class. Fix (cheap): name taxonomy.md and the preserved dispatch prompts among the artifacts that relocate with the verdict records; optionally have attestation echo the taxonomy's line count and source list.

**4. MINOR — SKILL §0's registrar definition contradicts its §5 use** (eval close-out assigns the registrar with a live lead). Widen the definition. **5. MINOR — phases/verification.md's Inputs list omits four items its own package hands out.** Complete it.

**6. ACCEPTED-CLASS — lead-held trust root** (gate copies, raw reports, record — fabrication lands in the countersign diff; within acceptance). Edge noted inside the acceptance: gate copies don't relocate at run-dir deletion, so post-hoc baseline audit dies with the run dir. **7. ACCEPTED-CLASS — money items marked inside dial §2's tier cells** (the dimension-keyed rule carries correctly elsewhere; within acceptance).

**What held under attack:** the eval path end-to-end; the quarantine letter as refined; the IOU letter and its three collectors; the fenced checks' delta readings (round 5's contradiction genuinely fixed); bidirectional cascade re-derivation; the reverse-provenance check; gates failing closed; ledger honesty including its two self-corrections.
