# Blind re-verification round 5 — lens 2: executability/buildability (verbatim report)

*Preserved verbatim per `verification_core.md` §6 rule 4. Dispatcher: the operator by standing
order; lead as mechanical proxy. Package: the full tree minus `verdicts/` and `.git`.*

---

**DISCLOSURE** — ambient context injected beyond this prompt and the listed files: the harness system prompt (env/cwd, scratchpad path, skills list), a system-reminder with user email and date, and a git-status block naming the branch and five recent commit subjects. My initial `find` also exposed `verdicts/` filenames (names only; contents unread). Nothing else opened outside the listed set.

# VERDICT: FAIL — 0 blockers, 3 majors, 8 minors

## Majors

**M1 — Architecture spot-verify cannot be dispatched legally with its own mandated lens:** step 10 names ideation coverage, whose briefed procedure requires the ideation file, but phase 2's input list explicitly excludes the raw ideation and rule 4 forbids adding to the package. Fix: scope the lens to its inventory/checkpoint-tracing half, or declare a spot-verify package for phase 2 that adds `reference/ideation.md` explicitly.

**M2 — Delta step 6 imports phase-6 duties its declared package and procedure cannot pay:** no specai NOTES.md (check 3 + §5.4's delta collector), no taxonomy at L (§2 rule 1), no NOTES seed / non-delta baselines (check 1's named artifacts), no templates/; and the package names "spot-verify records" no delta step produces — step 2 omits the spot-verify, so check 2's expected count has no derivable answer for a delta. Fix: a delta fenced-check variant (delta baselines, delta artifact names, which spot-verifies fire in a delta) and a completed step-6 package.

**M3 — The emitted-repo commit discipline has no emitted home:** change_rule §4 binds "emitted-repo builders never commit except under an explicit, gated standard … never by default" and claims instantiation "via `templates/start.md`" — but the template contains no occurrence of "commit". A builder operating from the emitted repo alone cannot know committing is the operator's act. Fix: add the commit line to the template.

## Minors

Unclosed parenthetical in phases/verification.md's package. Spot-record naming convention exists only inside phase-6's fenced check — name it where records are produced. delta step 2 has no gate-copy instruction. §4 tier M's lens split unspecified against "zero discretion" — state a default split or make it part of the templated dispatch. emission's exclusion parenthetical understates rule 2. exemplars §3 rule 4 assigns only the proof's verifier — who runs the `develop` builder is unstated. SKILL §5's run-closing duties (NOTES stamp, seed append) are barred to an eval run — assign eval close-out. S-tier reg/money=2 money-path changes owe a fresh verifier the S manual has no dispatch pattern for — give the template an S-tier fresh-dispatch line.

## Clean walks

`new product` at S: every consumed artifact has a prior producer; no missing lookups. `new product` at L incl. reg/money: REG produced before mapped; taxonomy has an empty-first-run rule; dimension-keyed trigger consistent. Reg/money at S: fires with no tier contradiction (modulo the S-dispatch minor). Phase-1/phase-3 spot-verifies: dispatch legal as written. Phase-6 seal: package, fenced checks, counts, judgment-gate shapes, sweeps — all dispatchable as written for a normal run. Crash-resume: every phase state reconstructible from disk. Eval route: exclusions self-consistent (operative rules travel in the dispatch prompt), briefs complete, degraded evals banned, honest interim verdict defined. Emitted-repo operability from templates alone: everything instantiates without factory files (except M3).
