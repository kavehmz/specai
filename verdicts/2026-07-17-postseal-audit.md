# Verdict record — post-seal audit over `d86eec3` · 2026-07-17

**What this is:** a four-lens fresh-context audit of the sealed tree, run at the operator's
invitation after the panel engagement closed — information, not a gate. **Dispatcher:** the
operator (in-session invitation); the lead relayed the same templated briefs as the engagement's
rounds. **Packages:** the full tree minus `verdicts/` and `.git`. Raw reports verbatim:
`2026-07-17-audit-lens{1,2,3,4}-*.md`.

**Verdicts: FAIL ×4 — 0 blockers, ~9 deduped majors, ~12 minors.** Roughly half the findings are
residue of the engagement's own final fix waves; half are multi-round survivors. The best
catches: the **money-path definition never reached the emitted world** (altitude M1 — omitted
WHAT on the highest-stakes surface, survived all six rounds) and **gate-copy tamper evidence
failed open on absence** (judgment M2 — a diff-invisible cheat outside the recorded acceptance).
What held: lens totality, the eval route end-to-end, the quarantine under grep, crash-resume,
emitted-repo self-sufficiency, and full ledger↔tree agreement on the engagement's closing claims.

## Triage (post-seal contract: fixes land via the change process, the seal is not reopened)

**All ~9 majors and ~12 minors fixed** in the follow-up change set:
- Money-path definition emitted verbatim (architecture template §7b closes with it; grammar §4.2
  mirrors; start template §4 keys to the section's presence) — emitted repos now self-carry it.
- Fenced check 1 derives the fired-gate set (dial §3 joins the package) and **absence blocks**;
  gate copies + a delta's change-request.md join §6.4's relocation set.
- Delta spot-verifies carry `change-request.md` (steps 1–2); the delta coverage lens gained both
  anti-laundering legs; the ideation-coverage reverse leg extends to identity marks; the appended
  repo-NOTES entry enumerates re-run phases (check 2's source).
- SKILL §5's eval close-out + §2's eval routing row joined the panel-gated class; the eval
  preamble sweep records into the run journal at preamble time; the comparison verdict's compiler
  is the operator; the panel floor is ≥2.
- README's "dispatcher" → "front door"; grammar §5's comms-matrix cells corrected; "motion
  intent" restored to the component template; exemplars.md declared in phase 2's inputs;
  notes-seed joins both mid-run spot packages; L partition assigned to the operator's seal order;
  §2 rule 1 defers to rule 4's package; remaining letter/label minors at their named homes.

**IOU (change_rule.md §5 rule 4):** these fixes touch rule-1 and rule-2 surfaces
(verification_core §5/§4, the fenced checks, change_rule §5.2 itself) · owed gate = focused pass
(rule-1 surfaces) + panel (rule-2 surfaces), verdicts returned and closed · anchor = **before the
next seal or eval** (the feedler build-proof or L4, whichever first). Recorded in NOTES.

**Standing accepted classes:** unchanged (lead-held trust roots with commit-diff backstop; dial
money-cell placement; prose density; the engagement's own close per acceptance (e)).
