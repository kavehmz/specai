# Post-seal audit — lens 4: judgment gate + adversarial (verbatim report)

*Preserved verbatim per `verification_core.md` §6 rule 4. Post-seal audit round over the sealed
tree (d86eec3). Package: the tree minus `verdicts/`/.git. Brief carried the operator's recorded
acceptances (a)–(e) with accepted-class severity, disclosed per §2 rule 4.*

---

**DISCLOSURE — ambient context injected beyond the prompt and listed files:** a system-reminder carrying the user's email and today's date; environment metadata including the git status block; the harness tool/skill listing; MCP server instructions. None was used as evidence. Two greps ran over the scoped files only.

# VERDICT: FAIL — 3 majors, 3 minors, accepted-class noted

**M1 — Eval close-out text is outside the panel gate its own class rule demands.** SKILL §5 is the sole home of "the run itself never touches NOTES or verdicts/" (exemplars §3 only bans reading), and SKILL §2's eval routing row sits in the same position — both gated at rule 1's single verifier while their sibling carriers (exemplars §3, the seal-operating text) are panel-gated. Fix: add them to rule 2's enumeration.

**M2 — Gate-copy tamper evidence fails open on absence** (not accepted-class: the cheat is invisible even in the commit-time diff). Check 1 diffs only what exists; no rule derives the expected gate set and blocks on an absent/empty gates/<phase>/ — the sibling mechanisms (spot records, IOUs) both block on absence. The verifier also cannot derive which gates fired: the package carries dial §2 only, and the tempo rule is §3. Fix: derive the fired-gate set from the tempo rule (add dial §3 to the package); absence blocks like an unpaid IOU.

**M3 — The delta coverage lens lacks the main path's anti-laundering hardenings**: no reverse-provenance leg, and any-mark non-goal accepted as a sink (the main row requires *revisitable*). Since the delta row replaces ideation coverage and travels alone in the brief, the closed laundering vector is open on the delta path. Fix: mirror both legs.

**m1** — the reverse-provenance leg covers revisitable non-goals only; identity non-goals carry permanent stop-power with no provenance check (backstop: the always-firing Definition gate). Extend to both marks. **m2** — §6.4's relocation list omits the gate copies; the baselines may vanish with the run dir, so the diff backstop never durably sees them. Add gates/**. **m3** — the eval-preamble sweep is recorded only in the comparison verdict, which doesn't exist if the eval aborts early; record in the run journal at preamble time.

**Accepted-class:** the engagement close itself (fixed-unconfirmed fixes accepted on direct diff review — acceptance (e), plainly disclosed by the ledger); lead-held trust roots (acceptance (a); M2 above is outside it); dial money-cell placement (acceptance (b)); prose density (acceptance (d)).

**Judgment:** the ledger's final entries agree with the binding tree everywhere checkable — the lens-totality rule, the gates/** diff, the emitted commit rule, the readable taxonomy carve-out, the panel-gated seal-operating text, and the corrected IOU payment letter are all present as stated; both ledger self-corrections are true against the tree. The quarantine holds under grep. The system is coherent and teachable; its residual weakness is exactly what the majors name: three sibling-path spots where a hardening exists on one route and not its twin.
